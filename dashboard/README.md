## Power BI Dashboard Information
- The folder titled `images` contains four images of the Power BI dashboard I created.
- The folder titled `power-bi-files` contains the Power BI project files.
- The files `feature_importances.csv`, `model_results.csv`, and the output `claims_predictions.csv` from my Jupyter Notebook (`insurance_claim_modeling.ipynb`) were imported into Power BI and used to create the dashboard.
